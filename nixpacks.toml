[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python init_db.py && python first_vector_embedding.py && streamlit run insurance_agents_main.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
